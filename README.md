in this you need to set the env in both backend as well as frontend 
in env you have to put your BACKEND_PORT and MONGO_URI in the backend folder 

and in frontend env Put PORT and REACT_APP_API_BASE_URL =
